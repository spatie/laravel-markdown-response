---
title: Requirements
weight: 2
---

This package requires PHP 8.4+ and Laravel 11 or 12.

The default `league` driver has no additional requirements. It ships as a hard dependency and works out of the box.

The `cloudflare` and `markdown-new` drivers require an internet connection and valid API credentials.
